------------------------------------------
------------------------------------ GRAPH 
Is is a non-linear datastructure
it is same as discrete mathematics graph

---------------------------------------------------------
------------------------------------ GRAPH REPRESENTATION
1. Adjacency Matrix 
- map indexes of the matrix
- O(v^2) space complexity


2. Adjacency List
- stored in a map
- how transition from one vertex(key) of the graph
- HashMap<Integer,List<Integer>> map
- O(V + E) space complexity
- V:vertices and E:edges

---------------------------------------------------------
---------------------------------------------------------

- to find shortest path we use BFS(level order)

- sirf directed acyclic graph (DAG) me topological sorting hoti h

- topological sorting using BFS is called Kahn's Algorithm

- indegree => if a node is child node of some other node then that node is called indegree

- toplogical order -> in degree wise 

- time complexity for Dijkstra Algorithm - V(logV + E.logV) -> E.V.logV

-------------------------------------------------
--------------------------------- BIPARTITE GRAPH
if a graph nodes are colored using 2-colors such that no two adjacent nodes have same color is called a bipartite graph

Graph with odd length cycle is a Non-Bipartite graph
Else every graph is bipartite graph

question like grouping/dividing a graph -> Bipartite question
ham graph ko do group me divide krte h yha pr, using 2 colors

----------------------------------------------------
--------------------------------- DISJOINT SET UNION
Disjoint Set -> when the intersection of two sets is null are called disjoint sets

Disjoint Set Union has two operations-
Also called Union Find Algorithm cuz it combines the set and finds the element
1. combine two given sets
2. tell if two members belong to same set or not

